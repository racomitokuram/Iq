name: blank

on:
  push:

jobs:
  universal:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        grab: ["prospect", "complete", "wicked", "privilege", "prove"]
        furnish: ["normally", "available", "defeat", "disguise"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar gg
